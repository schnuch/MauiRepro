<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiRepro.MainPage">

    <ScrollView HorizontalOptions="Center">
		<VerticalStackLayout
			Spacing="25"
			Padding="30"
			WidthRequest="400" >

			<Button
				x:Name="TestBtn"
				Text="Test Button"
				MinimumWidthRequest="200"
				IsEnabled="False" />

			<Entry
				Placeholder="Type to enable button"
				TextChanged="Enable_TextChanged" />

			<Entry
				Placeholder="Type to disable button"
				TextChanged="Disable_TextChanged" />

			<HorizontalStackLayout Spacing="25" HorizontalOptions="Center">
				<Button
					Text="Enable!"
					Clicked="Enable_Clicked" />
				<Button
					Text="Disable!"
					Clicked="Disable_Clicked" />
			</HorizontalStackLayout>

		</VerticalStackLayout>
    </ScrollView>

</ContentPage>
